// This app serves JavaScript through Vite and does not use Sprockets for
// application assets. Sprockets remains only for gem-provided assets, so this
// manifest is intentionally empty (Sprockets 4 requires the file to exist).
