require 'rails_helper'

module Genova
  module Slack
    module Command
      # describe Deploy do
      #   it 'should be return help message' do
      #     expect(message: "#{SlackRubyBot.config.user} deploy", channel: 'channel').to respond_with_slack_message(/I am ECS deploy Bot\./)
      #   end
      # end
    end
  end
end
